cc_binary(name = 'max_matching',
          srcs = ['max_matching.cc'],
          deps = ['//base/internal:base']
          )

cc_binary(name = 'code',
          srcs = ['code.cc'],
          deps = ['//base/internal:base']
          )
