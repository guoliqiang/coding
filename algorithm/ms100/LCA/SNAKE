cc_binary(name = 'LCA',
          srcs = ['LCA.cc'],
          deps = ['//base/internal:base']
          )

cc_binary(name = 'code',
          srcs = ['code.cc'],
          deps = ['//base/internal:base']
          )
